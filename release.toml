[workspace]
allow-branch = ["main"]
sign-commit = false
sign-tag = false
push = true
publish = false
tag-prefix = "v"
tag-name = "v{{version}}"
pre-release-commit-message = "chore: release v{{version}}"
tag-message = "v{{version}}"
